apiVersion: apps/v1
kind: Deployment
metadata:
  name: toolshare-lib-staging
  namespace: colossus-toolshare-lib-staging
  labels:
    app: toolshare-lib-staging
    managed-by: colossus
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: toolshare-lib-staging
  template:
    metadata:
      labels:
        app: toolshare-lib-staging
    spec:
      hostAliases:
        - ip: "192.168.50.250"
          hostnames:
            - "ubuntu.desmana-truck.ts.net"
      containers:
        - name: app
          image: ubuntu:30500/toolshare-lib-staging:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: app-secrets
                optional: true
